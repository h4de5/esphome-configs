substitutions:
  # ESP für mi flora
  devicename: "esp32_1"
  upper_devicename: "ESP32 1"
  mi_flora_mac1: !secret mi_flora_mac_1

<<: !include common/esp32.yaml

sensor:
  - !include common/base-sensor-wifi.yaml
  - !include common/base-sensor-uptime.yaml
  - platform: xiaomi_gcls002
    mac_address: "${mi_flora_mac1}"
    temperature:
      name: "${devicename} Temperature"
    moisture:
      name: "${devicename} Moisture"
    conductivity:
      name: "${devicename} Soil Conductivity"
    illuminance:
      name: "${devicename} Illuminance"
    # does no longer work
    # battery_level:
    #   name: "${devicename} Battery Level"
